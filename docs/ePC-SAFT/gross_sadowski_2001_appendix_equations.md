/begin{equation}
/tilde{a}^{res} = /frac{A^{res}}{N k T}
/tag{A.1}
/end{equation}

/begin{equation}
/tilde{a}^{res} = /frac{/hat{a}^{res}}{R T}
/tag{A.2}
/end{equation}

/begin{equation}
/tilde{a}^{res} = /tilde{a}^{hc} + /tilde{a}^{disp}
/tag{A.3}
/end{equation}

/begin{equation}
/tilde{a}^{hc} = /bar{m} /tilde{a}^{hs} - /sum_{i} x_{i} (m_{i} - 1) /ln g_{ii}^{hs}(/sigma_{ii})
/tag{A.4}
/end{equation}

/begin{equation}
/bar{m} = /sum_{i} x_{i} m_{i}
/tag{A.5}
/end{equation}

/begin{equation}
/tilde{a}^{hs} = /frac{A^{hs}}{N k T} = /frac{1}{/zeta_{0}}/left[ /frac{3/zeta_{1}/zeta_{2}}{1-/zeta_{3}} + /frac{/zeta_{2}^{3}}{/zeta_{3}(1-/zeta_{3})^{2}} + /left(/frac{/zeta_{2}^{3}}{/zeta_{3}^{2}} - /zeta_{0}/right) /ln(1-/zeta_{3}) /right]
/tag{A.6}
/end{equation}

/begin{equation}
 g_{ij}^{hs} = /frac{1}{1-/zeta_{3}} + /left(/frac{d_{i} d_{j}}{d_{i}+d_{j}}/right)/frac{3/zeta_{2}}{(1-/zeta_{3})^{2}} + /left(/frac{d_{i} d_{j}}{d_{i}+d_{j}}/right)^{2}/frac{2/zeta_{2}^{2}}{(1-/zeta_{3})^{3}}
/tag{A.7}
/end{equation}

/begin{equation}
/zeta_{n} = /frac{/pi}{6} F /sum_{i} x_{i} m_{i} d_{i}^{n}, /quad n /in /{0, 1, 2, 3/}
/tag{A.8}
/end{equation}

/begin{equation}
 d_{i} = /sigma_{i}/left[1 - 0.12 /exp/left(-/frac{3 /epsilon_{i}}{k T}/right)/right]
/tag{A.9}
/end{equation}

/begin{equation}
/eta = /zeta_{3}
/tag{A.10}
/end{equation}

/begin{equation}
 C_{1} = /left(1 + /bar{m}/frac{8/eta-2/eta^{2}}{(1-/eta)^{4}} + (1-/bar{m})/frac{20/eta-27/eta^{2}+12/eta^{3}-2/eta^{4}}{[(1-/eta)(2-/eta)]^{2}}/right)^{-1}
/tag{A.11}
/end{equation}

/begin{equation}
/overline{m^{2} /epsilon /sigma^{3}} = /sum_{i} /sum_{j} x_{i} x_{j} m_{i} m_{j}/left(/frac{/epsilon_{ij}}{k T}/right)/sigma_{ij}^{3}
/tag{A.12}
/end{equation}

/begin{equation}
/overline{m^{2} /epsilon^{2} /sigma^{3}} = /sum_{i} /sum_{j} x_{i} x_{j} m_{i} m_{j}/left(/frac{/epsilon_{ij}}{k T}/right)^{2}/sigma_{ij}^{3}
/tag{A.13}
/end{equation}

/begin{equation}
/sigma_{ij} = /frac{1}{2}(/sigma_{i} + /sigma_{j})
/tag{A.14}
/end{equation}

/begin{equation}
/epsilon_{ij} = /sqrt{/epsilon_{i} /epsilon_{j}} (1 - k_{ij})
/tag{A.15}
/end{equation}

/begin{equation}
I_{1}(/eta, /bar{m}) = /sum_{i=0}^{6} a_{i}(/bar{m}) /eta^{i}
/tag{A.16}
/end{equation}

/begin{equation}
I_{2}(/eta, /bar{m}) = /sum_{i=0}^{6} b_{i}(/bar{m}) /eta^{i}
/tag{A.17}
/end{equation}

/begin{equation}
a_{i}(/bar{m}) = a_{0i} + /frac{/bar{m}-1}{/bar{m}} a_{1i} + /frac{/bar{m}-1}{/bar{m}} /frac{/bar{m}-2}{/bar{m}} a_{2i}
/tag{A.18}
/end{equation}

/begin{equation}
b_{i}(/bar{m}) = b_{0i} + /frac{/bar{m}-1}{/bar{m}} b_{1i} + /frac{/bar{m}-1}{/bar{m}} /frac{/bar{m}-2}{/bar{m}} b_{2i}
/tag{A.19}
/end{equation}

/begin{equation}
F = /frac{6}{/pi} /eta /left(/sum_{i} x_{i} m_{i} d_{i}^{3}/right)^{-1}
/tag{A.20}
/end{equation}

/begin{equation}
/hat{F} = F N_{A} (10^{10} /AA{}//mathrm{m})^{3} (10^{-3} /mathrm{kmol}//mathrm{mol})
/tag{A.21}
/end{equation}

/begin{equation}
Z = 1 + /eta /left(/frac{/partial /tilde{a}^{res}}{/partial /eta}/right)_{T, x_{i}}
/tag{A.22}
/end{equation}

/begin{equation}
P = Z k T F (10^{10} /AA{}//mathrm{m})^{3}
/tag{A.23}
/end{equation}

/begin{equation}
Z = 1 + Z^{hc} + Z^{disp}
/tag{A.24}
/end{equation}

/begin{equation}
Z^{hc} = /bar{m} Z^{hs} - /sum_{i} x_{i} (m_{i} - 1) (g_{ii}^{hs})^{-1} F /frac{/partial g_{ii}^{hs}}{/partial F}
/tag{A.25}
/end{equation}

/begin{equation}
Z^{hs} = /frac{/zeta_{3}}{1-/zeta_{3}} + /frac{3/zeta_{1}/zeta_{2}}{/zeta_{0}(1-/zeta_{3})^{2}} + /frac{3/zeta_{2}^{3} - /zeta_{3} /zeta_{2}^{3}}{/zeta_{0}(1-/zeta_{3})^{3}}
/tag{A.26}
/end{equation}

/begin{equation}
F /frac{/partial g_{ij}^{hs}}{/partial F} = /frac{/zeta_{3}}{(1-/zeta_{3})^{2}} + /left(/frac{d_{i} d_{j}}{d_{i}+d_{j}}/right)/left(/frac{3/zeta_{2}}{(1-/zeta_{3})^{2}} + /frac{6/zeta_{2}/zeta_{3}}{(1-/zeta_{3})^{3}}/right) + /left(/frac{d_{i} d_{j}}{d_{i}+d_{j}}/right)^{2}/left(/frac{4/zeta_{2}^{2}}{(1-/zeta_{3})^{3}} + /frac{6/zeta_{2}^{2}/zeta_{3}}{(1-/zeta_{3})^{4}}/right)
/tag{A.27}
/end{equation}

/begin{equation}
Z^{disp} = -2/pi F /frac{/partial(/eta I_{1})}{/partial /eta} /overline{m^{2} /epsilon /sigma^{3}} - /pi F /bar{m}/left[ C_{1} /frac{/partial(/eta I_{2})}{/partial /eta} + C_{2} /eta I_{2} /right] /overline{m^{2} /epsilon^{2} /sigma^{3}}
/tag{A.28}
/end{equation}

/begin{equation}
/frac{/partial(/eta I_{1})}{/partial /eta} = /sum_{j=0}^{6} a_{j}(/bar{m}) (j + 1) /eta^{j}
/tag{A.29}
/end{equation}

/begin{equation}
/frac{/partial(/eta I_{2})}{/partial /eta} = /sum_{j=0}^{6} b_{j}(/bar{m}) (j + 1) /eta^{j}
/tag{A.30}
/end{equation}

/begin{equation}
C_{2} = /frac{/partial C_{1}}{/partial /eta} = -C_{1}^{2}/left[ /bar{m}/frac{-4/eta^{2} + 20/eta + 8}{(1-/eta)^{5}} + (1-/bar{m})/frac{2/eta^{3} + 12/eta^{2} - 48/eta + 40}{[(1-/eta)(2-/eta)]^{3}} /right]
/tag{A.31}
/end{equation}

/begin{equation}
/ln /phi_{k} = /frac{/mu_{k}^{res}(T,v)}{k T} - /ln Z
/tag{A.32}
/end{equation}

/begin{equation}
/frac{/mu_{k}^{res}(T,v)}{k T} = /tilde{a}^{res} + (Z - 1) + /left(/frac{/partial /tilde{a}^{res}}{/partial x_{k}}/right)_{T, v, x_{j /ne k}} - /sum_{j=1}^{N} x_{j} /left(/frac{/partial /tilde{a}^{res}}{/partial x_{j}}/right)_{T, v, x_{i /ne j}}
/tag{A.33}
/end{equation}

/begin{equation}
/zeta_{n,x_{k}} = /left(/frac{/partial /zeta_{n}}{/partial x_{k}}/right)_{T, F, x_{j /ne k}} = /frac{/pi}{6} F m_{k} d_{k}^{n}, /quad n /in /{0, 1, 2, 3/}
/tag{A.34}
/end{equation}

/begin{equation}
/left(/frac{/partial /tilde{a}^{hc}}{/partial x_{k}}/right)_{T, F, x_{j /ne k}} = m_{k} /tilde{a}^{hs} + /bar{m} /left(/frac{/partial /tilde{a}^{hs}}{/partial x_{k}}/right)_{T, F, x_{j /ne k}} - /sum_{i} x_{i} (m_{i} - 1) (g_{ii}^{hs})^{-1} /left(/frac{/partial g_{ii}^{hs}}{/partial x_{k}}/right)_{T, F, x_{j /ne k}}
/tag{A.35}
/end{equation}

/begin{equation}
/left(/frac{/partial /tilde{a}^{hs}}{/partial x_{k}}/right)_{T, F, x_{j /ne k}} = -/frac{/zeta_{0,x_{k}}}{/zeta_{0}} /tilde{a}^{hs} + /frac{1}{/zeta_{0}}/left[ /frac{3(/zeta_{1,x_{k}}/zeta_{2} + /zeta_{1}/zeta_{2,x_{k}})}{1-/zeta_{3}} + /frac{3/zeta_{1}/zeta_{2}/zeta_{3,x_{k}}}{(1-/zeta_{3})^{2}} + /frac{3/zeta_{2}^{2}/zeta_{2,x_{k}}}{/zeta_{3}(1-/zeta_{3})^{2}} + /frac{/zeta_{2}^{3}/zeta_{3,x_{k}}(3/zeta_{3}-1)}{/zeta_{3}^{2}(1-/zeta_{3})^{3}} + /left(/frac{3/zeta_{2}^{2}/zeta_{2,x_{k}}/zeta_{3}-2/zeta_{2}^{3}/zeta_{3,x_{k}}}{/zeta_{3}^{3}} - /zeta_{0,x_{k}}/right)/ln(1-/zeta_{3}) + /left(/zeta_{0} - /frac{/zeta_{2}^{3}}{/zeta_{3}^{2}}/right)/frac{/zeta_{3,x_{k}}}{1-/zeta_{3}} /right]
/tag{A.36}
/end{equation}

/begin{equation}
/left(/frac{/partial g_{ij}^{hs}}{/partial x_{k}}/right)_{T, F, x_{j /ne k}} = /frac{/zeta_{3,x_{k}}}{(1-/zeta_{3})^{2}} + /left(/frac{d_{i} d_{j}}{d_{i}+d_{j}}/right)/left(/frac{3/zeta_{2,x_{k}}}{(1-/zeta_{3})^{2}} + /frac{6/zeta_{2}/zeta_{3,x_{k}}}{(1-/zeta_{3})^{3}}/right) + /left(/frac{d_{i} d_{j}}{d_{i}+d_{j}}/right)^{2}/left(/frac{4/zeta_{2}/zeta_{2,x_{k}}}{(1-/zeta_{3})^{3}} + /frac{6/zeta_{2}^{2}/zeta_{3,x_{k}}}{(1-/zeta_{3})^{4}}/right)
/tag{A.37}
/end{equation}

/begin{equation}
/left(/frac{/partial /tilde{a}^{disp}}{/partial x_{k}}/right)_{T, F, x_{j /ne k}} = -2/pi F/left[ I_{1,x_{k}} /overline{m^{2} /epsilon /sigma^{3}} + I_{1} (/overline{m^{2} /epsilon /sigma^{3}})_{x_{k}} /right] - /pi F/left/{ [ m_{k} C_{1} I_{2} + /bar{m} C_{1,x_{k}} I_{2} + /bar{m} C_{1} I_{2,x_{k}} ] /overline{m^{2} /epsilon^{2} /sigma^{3}} + /bar{m} C_{1} I_{2} (/overline{m^{2} /epsilon^{2} /sigma^{3}})_{x_{k}} /right/}
/tag{A.38}
/end{equation}

/begin{equation}
(/overline{m^{2} /epsilon /sigma^{3}})_{x_{k}} = 2 m_{k} /sum_{j} x_{j} m_{j}/left(/frac{/epsilon_{kj}}{k T}/right)/sigma_{kj}^{3}
/tag{A.39}
/end{equation}

/begin{equation}
(/overline{m^{2} /epsilon^{2} /sigma^{3}})_{x_{k}} = 2 m_{k} /sum_{j} x_{j} m_{j}/left(/frac{/epsilon_{kj}}{k T}/right)^{2}/sigma_{kj}^{3}
/tag{A.40}
/end{equation}

/begin{equation}
C_{1,x_{k}} = C_{2}/zeta_{3,x_{k}} - C_{1}^{2}/left[ m_{k}/frac{8/eta-2/eta^{2}}{(1-/eta)^{4}} - m_{k}/frac{20/eta-27/eta^{2}+12/eta^{3}-2/eta^{4}}{[(1-/eta)(2-/eta)]^{2}} /right]
/tag{A.41}
/end{equation}

/begin{equation}
I_{1,x_{k}} = /sum_{i=0}^{6} /left[ a_{i}(/bar{m}) i /zeta_{3,x_{k}} /eta^{i-1} + a_{i,x_{k}} /eta^{i} /right]
/tag{A.42}
/end{equation}

/begin{equation}
I_{2,x_{k}} = /sum_{i=0}^{6} /left[ b_{i}(/bar{m}) i /zeta_{3,x_{k}} /eta^{i-1} + b_{i,x_{k}} /eta^{i} /right]
/tag{A.43}
/end{equation}

/begin{equation}
a_{i,x_{k}} = /frac{m_{k}}{/bar{m}^{2}} a_{1i} + /frac{m_{k}}{/bar{m}^{2}}/left(3 - /frac{4}{/bar{m}}/right) a_{2i}
/tag{A.44}
/end{equation}

/begin{equation}
b_{i,x_{k}} = /frac{m_{k}}{/bar{m}^{2}} b_{1i} + /frac{m_{k}}{/bar{m}^{2}}/left(3 - /frac{4}{/bar{m}}/right) b_{2i}
/tag{A.45}
/end{equation}

/begin{equation}
/frac{/hat{h}^{res}}{R T} = -T/left(/frac{/partial /tilde{a}^{res}}{/partial T}/right)_{F, x_{i}} + (Z - 1)
/tag{A.46}
/end{equation}

/begin{equation}
/frac{/hat{s}^{res}(P,T)}{R} = /frac{/hat{s}^{res}(v,T)}{R} + /ln(Z)
/tag{A.47}
/end{equation}

/begin{equation}
/frac{/hat{s}^{res}(P,T)}{R} = -T/left[/left(/frac{/partial /tilde{a}^{res}}{/partial T}/right)_{F, x_{i}} + /frac{/tilde{a}^{res}}{T}/right] + /ln(Z)
/tag{A.48}
/end{equation}

/begin{equation}
/frac{/hat{g}^{res}}{R T} = /frac{/hat{h}^{res}}{R T} - /frac{/hat{s}^{res}(P,T)}{R}
/tag{A.49}
/end{equation}

/begin{equation}
/frac{/hat{g}^{res}}{R T} = /tilde{a}^{res} + (Z - 1) - /ln(Z)
/tag{A.50}
/end{equation}

/begin{equation}
/left(/frac{/partial /tilde{a}^{res}}{/partial T}/right)_{F, x_{i}} = /left(/frac{/partial /tilde{a}^{hc}}{/partial T}/right)_{F, x_{i}} + /left(/frac{/partial /tilde{a}^{disp}}{/partial T}/right)_{F, x_{i}}
/tag{A.51}
/end{equation}

/begin{equation}
 d_{i,T} = /frac{/partial d_{i}}{/partial T} = /sigma_{i}/left(/frac{3/epsilon_{i}}{k T^{2}}/right)/left[-0.12 /exp/left(-/frac{3/epsilon_{i}}{k T}/right)/right]
/tag{A.52}
/end{equation}

/begin{equation}
/zeta_{n,T} = /frac{/partial /zeta_{n}}{/partial T} = /frac{/pi}{6} F /sum_{i} x_{i} m_{i} n d_{i,T} d_{i}^{n-1}, /quad n /in /{1, 2, 3/}
/tag{A.53}
/end{equation}

/begin{equation}
/left(/frac{/partial /tilde{a}^{hc}}{/partial T}/right)_{F, x_{i}} = /bar{m}/left(/frac{/partial /tilde{a}^{hs}}{/partial T}/right)_{F, x_{i}} - /sum_{i} x_{i} (m_{i} - 1) (g_{ii}^{hs})^{-1}/left(/frac{/partial g_{ii}^{hs}}{/partial T}/right)_{F, x_{i}}
/tag{A.54}
/end{equation}

/begin{equation}
/left(/frac{/partial /tilde{a}^{hs}}{/partial T}/right)_{F, x_{i}} = -/frac{/zeta_{0,T}}{/zeta_{0}} /tilde{a}^{hs} + /frac{1}{/zeta_{0}}/left[ /frac{3(/zeta_{1,T}/zeta_{2} + /zeta_{1}/zeta_{2,T})}{1-/zeta_{3}} + /frac{3/zeta_{1}/zeta_{2}/zeta_{3,T}}{(1-/zeta_{3})^{2}} + /frac{3/zeta_{2}^{2}/zeta_{2,T}}{/zeta_{3}(1-/zeta_{3})^{2}} + /frac{/zeta_{2}^{3}/zeta_{3,T}(3/zeta_{3}-1)}{/zeta_{3}^{2}(1-/zeta_{3})^{3}} + /frac{3/zeta_{2}^{2}/zeta_{2,T}/zeta_{3} - 2/zeta_{2}^{3}/zeta_{3,T}}{/zeta_{3}^{3}} /ln(1-/zeta_{3}) + /left(/zeta_{0} - /frac{/zeta_{2}^{3}}{/zeta_{3}^{2}}/right)/frac{/zeta_{3,T}}{1-/zeta_{3}} /right]
/tag{A.55}
/end{equation}

/begin{equation}
/frac{1}{2} d_{i} = /left(/frac{d_{i} d_{i}}{d_{i} + d_{i}}/right)
/tag{A.56}
/end{equation}

/begin{equation}
/frac{/partial g_{ii}^{hs}}{/partial T} = /frac{/zeta_{3,T}}{(1-/zeta_{3})^{2}} + /left(/frac{1}{2} d_{i,T}/right)/frac{3/zeta_{2}}{(1-/zeta_{3})^{2}} + /left(/frac{1}{2} d_{i}/right)/left(/frac{3/zeta_{2,T}}{(1-/zeta_{3})^{2}} + /frac{6/zeta_{2}/zeta_{3,T}}{(1-/zeta_{3})^{3}}/right) + /left(/frac{1}{2} d_{i} d_{i,T}/right)/frac{2/zeta_{2}^{2}}{(1-/zeta_{3})^{3}} + /left(/frac{1}{2} d_{i}/right)^{2}/left(/frac{4/zeta_{2}/zeta_{2,T}}{(1-/zeta_{3})^{3}} + /frac{6/zeta_{2}^{2}/zeta_{3,T}}{(1-/zeta_{3})^{4}}/right)
/tag{A.57}
/end{equation}

/begin{equation}
/left(/frac{/partial /tilde{a}^{disp}}{/partial T}/right)_{F, x_{i}} = -2/pi F/left(/frac{/partial I_{1}}{/partial T} - /frac{I_{1}}{T}/right)/overline{m^{2} /epsilon /sigma^{3}} - /pi F /bar{m}/left[ /frac{/partial C_{1}}{/partial T} I_{2} + C_{1} /frac{/partial I_{2}}{/partial T} - 2 C_{1} /frac{I_{2}}{T} /right]/overline{m^{2} /epsilon^{2} /sigma^{3}}
/tag{A.58}
/end{equation}

/begin{equation}
/frac{/partial I_{1}}{/partial T} = /sum_{i=0}^{6} a_{i}(/bar{m}) i /zeta_{3,T} /eta^{i-1}
/tag{A.59}
/end{equation}

/begin{equation}
/frac{/partial I_{2}}{/partial T} = /sum_{i=0}^{6} b_{i}(/bar{m}) i /zeta_{3,T} /eta^{i-1}
/tag{A.60}
/end{equation}

/begin{equation}
/frac{/partial C_{1}}{/partial T} = /zeta_{3,T} C_{2}
/tag{A.61}
/end{equation}

/begin{equation}
/sigma_{i} = q_{01} + /frac{M_{i} - M_{/mathrm{CH}_{4}}}{M_{i}} q_{11} + /frac{M_{i} - M_{/mathrm{CH}_{4}}}{M_{i}} /frac{M_{i} - 2 M_{/mathrm{CH}_{4}}}{M_{i}} q_{21}
/tag{B.1}
/end{equation}

/begin{equation}
/frac{m_{i}}{M_{i}} = q_{02} + /frac{M_{i} - M_{/mathrm{CH}_{4}}}{M_{i}} q_{12} + /frac{M_{i} - M_{/mathrm{CH}_{4}}}{M_{i}} /frac{M_{i} - 2 M_{/mathrm{CH}_{4}}}{M_{i}} q_{22}
/tag{B.2}
/end{equation}

/begin{equation}
/frac{/epsilon_{i}}{k} = q_{03} + /frac{M_{i} - M_{/mathrm{CH}_{4}}}{M_{i}} q_{13} + /frac{M_{i} - M_{/mathrm{CH}_{4}}}{M_{i}} /frac{M_{i} - 2 M_{/mathrm{CH}_{4}}}{M_{i}} q_{23}
/tag{B.3}
/end{equation}
